<table>
  <thead>
    <tr>
      <th width="100">Date</th>
      <th>Type</th>
      <th width="150">Next Due</th>
      <th></th>
    </tr>
  </thead>
    <tbody>
    <% pet.immunizations.each do |p| %>
<tr>
  <td><%= p.date %></td>
  <td><%= p.itype %></td>
  <td><%= p.next_due %></td>
  <td><%= link_to 'Edit', edit_user_pet_immunization_path(pet.user_id, pet, p.id)%> <%= link_to 'Delete', user_pet_immunization_path(pet.user_id, pet, p.id), confirm: "Are you sure?", method: :delete%></td>
</tr>
<% end %>
</tbody>
</table>